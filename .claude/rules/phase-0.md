# Phase 0 Rules

We are in Phase 0 (Design & Research). The rules for this phase:

- Do NOT write implementation code without an ADR first.
- The previous session's scaffold in `crates/` is a DRAFT — it may change based on research.
- Every major technical choice needs research logged in `docs/research/` before deciding.
- Use `/research` skill to investigate questions from `docs/MASTER_PLAN.md` section 4.
- Use `/adr-create` skill to formalize decisions.
- Use `/phase-check` skill to track progress.
- Reference RUSVEL's working code at `/Users/bm/rusvel` when comparing approaches.
- Exit criteria: all 8 decisions in MASTER_PLAN.md resolved with ADRs, one WIT proof-of-concept validates the WASM approach.
